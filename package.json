{
  "name": "key-collective",
  "version": "0.2.0",
  "description": "Cloudflare-native multi-tenant LLM router SaaS",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gate": "npm run typecheck && npm test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.9.3",
    "vitest": "^2.0.5",
    "wrangler": "^3.75.0"
  }
}
