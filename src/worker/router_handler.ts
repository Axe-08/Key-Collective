/**
 * Key Collective v2/v4 — Router Handler Smart Re-Export Façade
 * Maintains 100% backward compatibility for all external callers and tests.
 * All core routing logic has been decoupled into the `src/worker/router/` subsystem.
 */

export * from "./router/index";
